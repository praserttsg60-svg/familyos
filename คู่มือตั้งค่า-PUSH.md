# วิธีเปิดระบบแจ้งเตือน Push ของ Family OS

> ทำครั้งเดียวจบ ใช้เวลาประมาณ 10 นาที
> ทำตามลำดับเป๊ะ ๆ ห้ามข้ามข้อ

---

## ⚠️ สาเหตุที่ตอนนี้ขึ้นว่า "VAPID key pair or subject is invalid"

ข้อความนี้แปลว่า **ตั้งค่าครบทั้ง 3 ตัวแล้ว แต่ค่าที่ใส่ไปไม่ถูกต้อง** สาเหตุที่พบบ่อยเรียงจากมากไปน้อย:

1. **VAPID_SUBJECT ไม่ได้ขึ้นต้นด้วย `mailto:`** ← เจอบ่อยที่สุด
   ใส่แค่ `pond@gmail.com` ไม่ได้ ต้องเป็น `mailto:pond@gmail.com`
2. **กุญแจสาธารณะกับกุญแจส่วนตัวไม่ใช่คู่เดียวกัน** — สร้างคนละครั้งกัน หรือคัดลอกมาจากคนละที่
3. **มีช่องว่างหรือขึ้นบรรทัดใหม่ติดมาตอนวาง** — มองไม่เห็นด้วยตาแต่ทำให้พัง
4. **คัดลอกมาไม่ครบ** กุญแจสาธารณะยาว 87 ตัวอักษร ส่วนตัวยาว 43 ตัวอักษร

**เวอร์ชันใหม่นี้แก้ให้แล้ว 2 อย่าง:** ระบบจะตัดช่องว่างให้อัตโนมัติ และจะบอกชัดเจนว่าผิดตรงไหน แทนข้อความกำกวมแบบเดิม

---

## 🔑 ขั้นที่ 1 — กุญแจที่สร้างให้แล้ว (ใช้ชุดนี้ได้เลย)

ผมสร้างคู่กุญแจที่ถูกต้อง 100% ให้แล้ว ตรวจสอบแล้วว่าเป็นคู่กันจริง ความยาวถูกต้อง และอยู่ในรูปแบบ base64url

```
VAPID_PUBLIC_KEY
BGh921Eht1Eu-ps_--_E7C3OACQmlz705ZQCyBW9NW_CplGJQ5pJBnXeTcAS2SFkWRiqhekyozH3jJWivzU-uvs
```

```
VAPID_PRIVATE_KEY
APsdlo3JAuc0Xv2aDNI22nWSjaTmKxPgWDsH0nA0U64
```

```
VAPID_SUBJECT
mailto:อีเมลจริงของคุณ@gmail.com
```

> **VAPID_SUBJECT ต้องเปลี่ยนเป็นอีเมลของคุณเอง** และต้องมีคำว่า `mailto:` นำหน้าเสมอ
> ใช้อีเมลอะไรก็ได้ที่ติดต่อคุณได้จริง บริการ Push อาจใช้ติดต่อถ้ามีปัญหา

**เรื่องความปลอดภัย:** กุญแจส่วนตัวนี้ใช้ได้กับเว็บคุณเว็บเดียวและใช้เซ็นข้อความแจ้งเตือนเท่านั้น ไม่เกี่ยวกับข้อมูลครอบครัวในเครื่องผู้ใช้ ถ้าอยากสร้างเองใหม่ให้ดูภาคผนวกท้ายเอกสาร

---

## 📤 ขั้นที่ 2 — ต้องอัปขึ้นด้วย Git ไม่ใช่การลากวาง

**นี่คือจุดที่คนพลาดมากที่สุด และเป็นสาเหตุที่ Push ไม่เคยทำงานเลย**

Family OS เวอร์ชันนี้มี **Netlify Functions** ซึ่งต้องติดตั้งไลบรารี `web-push` และ `@netlify/blobs` ก่อนใช้งาน

- **การลากโฟลเดอร์วางที่ Netlify Drop → ❌ ใช้ไม่ได้** เพราะ Netlify จะไม่ติดตั้งไลบรารีให้ ฟังก์ชันจะพังทั้งหมด
- **ต้องเชื่อมผ่าน GitHub → ✅ Netlify จะรัน `npm install` ให้เอง**

### วิธีทำ (ไม่ต้องใช้คำสั่งอะไรเลย ทำผ่านหน้าเว็บได้)

1. สมัคร **github.com** (ฟรี)
2. กด **New repository** → ตั้งชื่อ `familyos` → เลือก **Private** → **Create**
3. ในหน้า repo กด **uploading an existing file**
4. **ลากไฟล์ทั้งหมดในโฟลเดอร์นี้เข้าไป รวมถึงโฟลเดอร์ `netlify` ด้วย** → กด **Commit changes**
5. เข้า **Netlify** → **Add new site** → **Import an existing project** → **GitHub** → เลือก repo `familyos`
6. หน้าตั้งค่า build ปล่อยค่าว่างไว้ตามที่ระบบเติมให้ (มี `netlify.toml` กำหนดไว้แล้ว) → กด **Deploy**

> ถ้าคุณมีเว็บ Family OS เดิมที่ลากวางไว้แล้ว ให้สร้างไซต์ใหม่จาก GitHub แทน แล้วค่อยเปลี่ยนชื่อไซต์ทีหลัง

---

## 🔧 ขั้นที่ 3 — ใส่ Environment Variables

1. ใน Netlify เข้าไซต์ของคุณ → **Site configuration** → **Environment variables**
2. กด **Add a variable** → **Add a single variable**
3. ใส่ทีละตัวรวม 3 ตัว:

| Key | Value |
|---|---|
| `VAPID_PUBLIC_KEY` | (คัดลอกจากขั้นที่ 1) |
| `VAPID_PRIVATE_KEY` | (คัดลอกจากขั้นที่ 1) |
| `VAPID_SUBJECT` | `mailto:อีเมลคุณ@gmail.com` |

**ข้อควรระวังตอนวาง**
- อย่าใส่เครื่องหมายคำพูด `"` ครอบค่า
- อย่าให้มีช่องว่างหน้าหรือหลัง
- อย่ากด Enter ต่อท้าย
- Scopes เลือก **All scopes** / Values เลือก **Same value for all deploy contexts**

4. **สำคัญมาก: ต้อง Deploy ใหม่หลังใส่ค่า** ไปที่ **Deploys** → **Trigger deploy** → **Clear cache and deploy site**
   ถ้าไม่ทำขั้นนี้ ค่าที่ใส่จะยังไม่มีผล

---

## ✅ ขั้นที่ 4 — ตรวจว่าใช้ได้จริง

เปิดเว็บของคุณแล้วต่อท้าย URL ด้วย:

```
/.netlify/functions/push-status
```

**ถ้าถูกต้อง** จะเห็นแบบนี้
```json
{"ok":true,"configured":true,"lengths":{"publicKey":87,"privateKey":43}}
```

**ถ้ายังผิด** จะบอกสาเหตุเป็นภาษาไทยชัดเจน เช่น
```json
{"detail":"VAPID_SUBJECT ต้องขึ้นต้นด้วย mailto: ..."}
```
อ่านช่อง `detail` แล้วแก้ตามนั้นได้เลย

---

## 📱 ขั้นที่ 5 — เปิดใช้บนมือถือ

### iPhone (ต้องทำครบทุกข้อ ไม่งั้นไม่ขึ้น)
1. เปิดเว็บ **ด้วย Safari เท่านั้น** (ไม่ใช่จาก LINE หรือ Chrome)
2. กดปุ่มแชร์ ⬆️ → **เพิ่มไปยังหน้าจอโฮม** → **เพิ่ม**
3. **ปิด Safari แล้วเปิดแอปจากไอคอนบนหน้าจอโฮม** ← ข้อนี้ห้ามข้าม
4. เข้า ตั้งค่า → **เปิดการแจ้งเตือนบนมือถือ** → อนุญาต
5. กด **ทดสอบแจ้งเตือนจริง** ต้องเด้งขึ้นมา

> iPhone รองรับ Push บนเว็บตั้งแต่ iOS 16.4 ขึ้นไป **และเฉพาะตอนเปิดจากไอคอนหน้าจอโฮมเท่านั้น** เปิดใน Safari ธรรมดาจะไม่มีทางได้รับ

### Android
เปิดใน Chrome → เมนู ⋮ → เพิ่มลงในหน้าจอหลัก → เปิดจากไอคอน → กดเปิดการแจ้งเตือน

---

## ⏰ ระบบแจ้งเตือนทำงานยังไง

`scheduled-reminders` จะรัน **ทุกชั่วโมงโดยอัตโนมัติ** ตรวจว่ามีรายการไหนถึงกำหนดหรือใกล้ถึง แล้วส่ง Push ไปที่เครื่องที่สมัครไว้ **แม้ปิดแอปอยู่ก็ได้รับ**

รายการของคุณจะถูกส่งไปเก็บที่เซิร์ฟเวอร์เฉพาะ **ชื่อเรื่อง วันครบกำหนด และการตั้งเตือน** เท่านั้น เพื่อให้ระบบรู้ว่าต้องเตือนเมื่อไหร่ — ข้อมูลอื่นยังอยู่ในเครื่องคุณเหมือนเดิม **ข้อนี้ควรบอกผู้ใช้ให้ชัด** เพราะต่างจากตอนที่ยังไม่มี Push

---

## 🧯 ถ้ายังไม่ได้ ให้ไล่ตรวจตามนี้

| อาการ | สาเหตุ | วิธีแก้ |
|---|---|---|
| `/.netlify/functions/push-status` ขึ้น 404 | อัปด้วยการลากวาง ไม่ได้ผ่าน GitHub | ทำขั้นที่ 2 ใหม่ |
| ขึ้น 500 หรือ `Cannot find module 'web-push'` | Netlify ยังไม่ได้ติดตั้งไลบรารี | เช็คว่ามี `package.json` ใน repo แล้ว Clear cache and deploy |
| `detail` บอกว่า subject ผิด | ลืมใส่ `mailto:` | แก้แล้ว deploy ใหม่ |
| `detail` บอกความยาวไม่ถูก | คัดลอกไม่ครบ | คัดลอกใหม่ทั้งบรรทัด |
| ทุกอย่าง ok แต่ iPhone ไม่เด้ง | ยังเปิดจาก Safari ไม่ได้เปิดจากไอคอน | ทำขั้นที่ 5 ข้อ 2-3 ใหม่ |
| Android ไม่เด้ง | ปิดการแจ้งเตือนไว้ในตั้งค่าเครื่อง | ตั้งค่า → แอป → Family OS → การแจ้งเตือน → เปิด |

---

## ภาคผนวก — ถ้าอยากสร้างกุญแจเอง

บนคอมที่มี Node.js ติดตั้งอยู่ พิมพ์คำสั่งนี้:

```
npx web-push generate-vapid-keys
```

จะได้ Public Key กับ Private Key มาเป็นคู่ ให้ใช้ทั้งคู่จากการรันครั้งเดียวกันเท่านั้น
**ห้ามเอา public จากครั้งหนึ่งมาผสมกับ private จากอีกครั้ง** เพราะจะเกิด error เดิมทันที
